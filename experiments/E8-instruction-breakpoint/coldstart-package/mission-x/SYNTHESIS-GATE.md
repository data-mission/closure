# MISSION X — SYNTHESIS GATE (adjudicated; all five audit inputs in)

Status: COMPLETE as of 2026-07-18T23:30Z. Inputs: P1 completeness, P2 design, P3 necessity,
P4 connections, X1 anatomy (all delivered; conflicts adjudicated below by lead against raw data).
Base: closure@689833c + uncommitted mission-x updates. Owner: everything authorized, no ceiling,
per-launch disclosure, exploratory grade. ⚑ = owner decision points.

## 0. Adjudicated ground truth (final)

1. **A3 real contamination = 0/200** (X1, cross-validated 2 blind auditors + hand checks + lead
   3/3 raw reads). The ~26% "floor" was instrument false-positives: 171 template-collisions
   (shared "{number} {verdict} {threshold}" scaffold clears NLI 0.7), 26 supersession-mentions,
   3 unit-collisions. All 200 outputs acknowledged and integrated the correction. Flag rate
   tracks template shape (judgment items 47.7%, numeric-only 1.2%), not model behavior.
2. **A1 measures revision success** (polarity inversion, 450/450). P4's and partly P3's
   "failure-at-ceiling" readings REJECTED; P1's reading accepted. No E8 depth-failure evidence exists.
3. **A2 clean null** (drops 5.0/4.6/5.4% vs θ=10%, correct side after the counting fix).
4. **Convergent conclusion: instructed revision is ROBUST on every validly measured corpus in the
   program** (E5 B=0.9%; A2 clean; A3 ≈0 real; A1 ~95% success). E8 outcome (b) confirmed in the
   strongest form. THE OPERATOR LINE HAS NO OPENING IN EXISTING DATA.
5. **The only measured revision failure anywhere in the program is E5 Arm C itself** — the
   contraction operator's 10.3% injected contamination. Its reality is under re-examination with
   the same discriminator (could be template artifact too). Either answer is load-bearing.
6. Four instrument↔corpus coupling errors found today, all pre-publication: A2 wrong-side
   counting, A1 polarity inversion, A3 template collision, (E5-C pending). The measuring
   instrument, not the model, was the modal failure class of the whole revision line.

## 1. Gap inventory × coverage (P1 spine, adjudicated + pivoted)

G1 NLI never human-validated → **X-HUMAN adopted, upgraded from "gate" to NECESSARY**
   (today proved the instrument fails silently; ≥100 items × 2 annotators, κ + FP/FN near 0.7;
   also validates instrument-v2 and X6's action oracles).
G2 A1 polarity → X4 (with P2's insurance-item fix: transform primary, DROP/regen insurance,
   fail-closed stale-value assertions). Closes.
G3/G7 512-bound + context axis → X0 RESPECIFIED per P2: NOT per-segment (pre-rejected,
   inflation 0.643→0.787 is the acceptance test) but a long-context NLI checkpoint amendment,
   full E5-corpus equivalence + planted-fixture soundness green. Until then context axis stays out.
G4 single-vendor → X-VENDOR adopted (A2/A3-class rerun, second generation vendor).
G5 LLM-corpus circularity → PARTIAL: instrument-v2 + X-HUMAN reduce judge circularity;
   corpus authorship circularity remains → future non-synthetic corpus (⚑ optional add).
G6 shallowness → carried by the post-pivot "harder non-templated corpora" line (X1's rec #3).
G8 marginal-power band → ⚑ moot for now (no failing regime to power against); revisit if
   E5-C re-exam or new corpora reopen a regime.
G9 A3 floor unexplained → CLOSED by X1 (explained: artifact).
G10/G11 operator evidence → X3 RETIRED AS SCOPED (no failing regime). The operator question
   survives only via: (a) E5-C re-exam (operator-harm reality), (b) X5/H-COMPACT, (c) any
   genuine failure found on harder corpora later.
G12 behavioral form → X6 (paired positive/negative action traces per P2; own validation via X-HUMAN pattern).

## 2. Connection graph (P4 spine, corrected)

P4's graph adopted with corrections: E4' A1→X4 edge = polarity repair only; all A3-floor
edges (E5,E6,E7) re-terminate on "A3 floor = artifact" (X1). X3-WIN/LOSS branches replaced by:
- **RESOLVED 2026-07-19: E5-C-ARTIFACT branch FIRED.** Arm-C re-exam (instrument-v2 discriminator,
  2 independent passes, lead-verified evidence table): 0/11 real echo — 9 FP-INTEGRATED (Arm C asserts
  the CORRECTED outcome; NLI fired on historical/superseded scaffold), 2 DANGLING_RULE (corrected value
  computed, rule retained, final Boolean unstated — a real contraction-COMPLETENESS defect, not
  contamination). E5's registered B-vs-C separation (0.9% vs 10.3%) is an instrument artifact of the
  same template-collision class. **E5's H-RELEASE refutation REOPENS.** ⚑ OWNER: public correction
  note for the E5 record (his act, per standing rule). Distinct actionable: fix contraction's
  rule-re-application gap before any operator retest. Instrument-v2 ACCEPTED (4/4 gates; adversarial
  positive controls 60/60, 40/40, 30/30 — it can fire). Count-basis (adjudicated, dash-owner flag): VERDICT basis = 756 must_change items (post-pruning
  trials, = per_dose sums 150+262+344) — the canonical number; sweep basis incl. the 30 pruned
  items = 786. "1,428" mixed both annotation sides — never use it for contamination. 0 real on
  every basis.
  **B-ARM CONTROL (airtight closure):** E5 Arm-B's single flag (F1-0016/1) also = FP-INTEGRATED —
  and the SAME item flags in BOTH arms, proving the metric tracks sentence template, not behavior.
  All three E5 arms ≈0 real; C's higher raw count reflects contraction's OUTPUT SHAPE (retains
  superseded claims as explicit contrasts → more scaffold premises for max-over-premises NLI), not
  contamination. The registered B-vs-C separation does not exist under a valid instrument.
Mission residual (P4, stands): revision subgraph closes; E0–E3, E4-enforce, E6/E7 untouched.
⚑ "All dots of closure" requires adding E4, E6/E7, E3-registered campaigns.

## 3. Plan of record (post-pivot)

CRITICAL PATH (zero spend, local): instrument-v2 (claim-grounded value/verdict scoring;
acceptance: 200→0 on A3 flags AND 10/10 synthetic true-echo positive controls — must be able
to fire) → full A3 rescore (1,428 items, final table) → **E5 Arm-C re-examination** (the
program's remaining measured failure: real or artifact).
PARALLEL BUILDS (authorized, in order of value): X4 (A1 rebuild w/ insurance fix → gen ≈$9.5 →
GPU score), X5/E9 (compaction as operator-BEARING hypothesis, no-compaction baseline, numeric-only
scored strings), X-HUMAN (protocol + sampling; annotation is owner-mediated ⚑), X6 (behavioral
instrument prototype), X-VENDOR (after instrument-v2 exists to grade it), X0 (checkpoint
amendment, soundness-gated). RETIRED: X3-as-scoped, X2 (no failing regime to bridge; P3+P4
decorative verdicts stand). Dropped-axes disposition: context→X0, compaction→X5, behavioral→X6;
correction-of-correction + distance stay retired (design confounds); domain-shift retired.

## 4. Pre-stated conclusions (two-sided, forced by data) — RESOLVED 2026-07-19

Which branch fired for each (actual outcomes; full numbers in MISSION-X-VERDICT.md):
- instrument-v2/A3-rescore: **NEAR-0 REAL FIRED** → 0/786 real; instructed revision robust on templated
  corpora, E8 (b) reinforced.
- E5-C re-exam: **ARTIFACT FIRED** → 0/11 real echo; E5 B-vs-C separation reopens; H-RELEASE REOPENED ⚑
  (correction note drafted; publication = owner).
- X4: **CLEAN NULL FIRED** → A1 depth NO BREAK, true floor 0/447; depth question settled (revision robust
  across depth).
- X5/E9: **HOLD FIRED** → H-COMPACT holds clean, 0/1800 real; corrections survive compaction; E5's
  single-shot result does NOT fail to scale (the operator does not accumulate error at this operating
  point). NB the frozen-NLI showed the rising-S-curve that WOULD have falsely confirmed accumulation —
  caught by the screen.
- X6: **NEITHER — PILOT VOID** → a corpus-construction defect (missing per-case facts) made the pilot
  uninterpretable; the harness correctly WITHHELD; the model's refusal-to-fabricate was the good
  behavior. Behavioral-form question remains OPEN pending an ⚑ owner-gated re-run.
- X-HUMAN: PENDING → annotation packets (773 + 30 items) staged, awaiting annotators ⚑.

PROGRAM CONCLUSION (forced by all outcomes): the operator line has NO measured opening in ANY regime
tested — single-shot (E5), depth (X4), or compaction (E9). Every failure signal across the campaign was
an instrument/harness/corpus artifact, not model behavior.

## 5. Execution ledger

23:26Z A1 validity disclosure in canonical files (diff-verified number-neutral).
23:28Z instrument-v2 + rescores assigned (zero spend). Spend to date Mission X: $0.
Pending spends: X4 gen ≈$9.5; X5 corpus gen TBD; X-VENDOR TBD — each disclosed at launch.
